# raw/ — 原データ置き場(Git管理外)

このディレクトリの内容は README を除き `.gitignore` で **Git 管理外** です。

- 実際の過去問本文・市販教材・有償資料など、著作権未確認のデータはここに置いてもコミットされません
- ここへデータを置いた場合も、`../metadata/sources.yaml` に出典記録を必ず作成してください
- 著作権・ライセンスの確認が取れたデータのみ、正規化して `../normalized/` にコミットできます
